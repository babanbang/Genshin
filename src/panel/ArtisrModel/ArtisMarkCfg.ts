import { Avatar } from "../Avatar"

export const ArtisMarkCfg = {
	getCfg(avatar: Avatar) {
		const { attrWeight, title } = ArtisMarkCfg.getCharArtisCfg(avatar)
	},

	getcharArtisCfg(avatar: Avatar) {
		const { attr, weapon, elem, char, Artis } = avatar


	}
}